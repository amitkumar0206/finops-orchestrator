{
    "commands": [
        "set -e",
        "cd /opt/aasmaa-demo",
        "grep -q '^      SECRET_KEY:' docker-compose.yml || awk '/ENVIRONMENT:/{print; print \"      SECRET_KEY: temporary-demo-secret-key-change-in-production\"; next}1' docker-compose.yml > docker-compose.yml.tmp && mv docker-compose.yml.tmp docker-compose.yml",
        "docker-compose up -d backend",
        "sleep 8",
        "docker ps -a --filter name=aasmaa-backend --format \"table {{.Names}}\\t{{.Image}}\\t{{.Status}}\"",
        "docker inspect aasmaa-backend --format \"{{range .Config.Env}}{{println .}}{{end}}\" | grep -E \"SECRET_KEY=|DATABASE_ENABLED=|ATHENA_WORKGROUP=|AWS_CUR_DATABASE=|AWS_CUR_TABLE=\" || true",
        "curl -sS http://localhost:8000/health",
        "docker logs --tail 60 aasmaa-backend"
    ]
}